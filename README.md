
  # High-fidelity web app prototype

  This is a code bundle for High-fidelity web app prototype. The original project is available at https://www.figma.com/design/bqlAGQIln6AOSuISAPqwpn/High-fidelity-web-app-prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  